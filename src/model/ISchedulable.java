package model;
import java.util.Date;
public interface ISchedulable {
    void schedule(Date date, String time);          // Esta interfaz va a contener un método, este método va a recibir como parámetros un objeto tipo Date y un String (time), no lo tengo que implementar

}
